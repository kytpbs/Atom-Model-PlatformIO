#include "strip.h"

extern NeoElectrons smallStrip;  // Create the small strip object
extern NeoElectrons innerStrip;  // Create the inner strip object
extern NeoElectrons innerStrip2; // Create the second inner strip object
extern NeoElectrons outerStrip;  // Create the outer strip object
extern NeoElectrons outerStrip2; // Create the second outer strip object
extern NeoElectrons outerStrip3; // Create the third outer strip object
